# Array
## Description
Array is a data structure that stored data in **contiguous memory locations**,every data can be accessed by using index.
Array's size is static,and all elements need to same data type.

## Abstract Data Type
Array's operation:

